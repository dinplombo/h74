#ifndef HELPERS_HPP
#define HELPERS__HPP

#include <cstddef> //size_t

namespace helpers
{

void swap(int& a_first, int& a_second);

template <typename T>
int sum_action(T& a_value, T& a_sum);

size_t artimetic_sequence(size_t a_dim);



}//namespace helpers





#endif // HELPERS_L_HPP
